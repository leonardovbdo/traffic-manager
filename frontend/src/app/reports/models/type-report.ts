/**
 * Interface que define a estrutura de um tipo de ocorrência.
 * Contém propriedades como ID (`id`) e descrição (`description`).
 */
export interface TypeReport {
    id: number;
    description: string;
}
